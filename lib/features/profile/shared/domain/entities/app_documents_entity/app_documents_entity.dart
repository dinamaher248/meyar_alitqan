class AppDocumentsEntity {
  String ? id;
  String ? doc_key;
  String ? lang;
  String ? content;
  String ? createdAt;
  String ? updatedAt;
  AppDocumentsEntity({this.id, this.doc_key, this.lang, this.content, this.createdAt, this.updatedAt});
}